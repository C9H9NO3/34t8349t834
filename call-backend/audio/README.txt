Put your call MP3s in this folder.

By default the backend looks for:
  greeting.mp3   - played first when you hit "Play greeting" / "Run sequence"
  followup.mp3   - played after the delay, or via "Play follow-up"

You can change the filenames in config.js (greetingFile / followupFile).
